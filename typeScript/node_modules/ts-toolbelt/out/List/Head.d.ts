import { Length } from 'ts-toolbelt/out/List/Length';
import { List } from 'ts-toolbelt/out/List/List';
/**
 * Get the first entry of `L`
 * @param L to extract from
 * @returns [[Any]]
 * @example
 * ```ts
 * ```
 */
export declare type Head<L extends List> = Length<L> extends 0 ? never : L[0];
