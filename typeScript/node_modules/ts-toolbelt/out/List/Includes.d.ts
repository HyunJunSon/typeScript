import { Match } from 'ts-toolbelt/out/Any/_Internal';
import { Includes as OIncludes } from 'ts-toolbelt/out/Object/Includes';
import { ObjectOf } from 'ts-toolbelt/out/List/ObjectOf';
import { List } from 'ts-toolbelt/out/List/List';
/**
 * Check whether `L` has entries that match `M`
 * @param L to be inspected
 * @param M to check entry type
 * @param match (?=`'default'`) to change precision
 * @returns [[Boolean]]
 * @example
 * ```ts
 * ```
 */
export declare type Includes<L extends List, M extends any, match extends Match = 'default'> = OIncludes<ObjectOf<L>, M, match>;
