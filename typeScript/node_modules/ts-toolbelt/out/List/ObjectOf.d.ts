import { _Omit } from 'ts-toolbelt/out/Object/Omit';
import { _Pick } from 'ts-toolbelt/out/Object/Pick';
import { Length } from 'ts-toolbelt/out/List/Length';
import { List } from 'ts-toolbelt/out/List/List';
/**
 * Transform a [[List]] into an [[Object]] equivalent
 * @param L to transform
 * @returns [[Object]]
 * @example
 * ```ts
 * ```
 */
export declare type ObjectOf<O extends List> = O extends unknown ? number extends Length<O> ? _Pick<O, number> : _Omit<O, keyof any[]> : never;
