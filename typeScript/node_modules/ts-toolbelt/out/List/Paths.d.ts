import { Paths as OPaths } from 'ts-toolbelt/out/Object/Paths';
import { ObjectOf } from 'ts-toolbelt/out/List/ObjectOf';
import { List } from 'ts-toolbelt/out/List/List';
/**
 * Get all the possible paths of `L`
 * (⚠️ this won't work with circular-refs)
 * @param L to be inspected
 * @returns [[String]][]
 * @example
 * ```ts
 * ```
 */
export declare type Paths<L extends List> = OPaths<ObjectOf<L>>;
