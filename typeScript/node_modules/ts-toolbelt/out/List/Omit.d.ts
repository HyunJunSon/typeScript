import { _Omit as _OOmit } from 'ts-toolbelt/out/Object/Omit';
import { _ListOf } from 'ts-toolbelt/out/Object/ListOf';
import { Key } from 'ts-toolbelt/out/List/_Internal';
import { List } from 'ts-toolbelt/out/List/List';
import { ObjectOf } from 'ts-toolbelt/out/List/ObjectOf';
/**
 * @hidden
 */
export declare type _Omit<L extends List, K extends Key> = _ListOf<_OOmit<ObjectOf<L>, `${K & number}` | K>>;
/**
 * Remove out of `L` the entries of key `K`
 * @param L to remove from
 * @param K to chose entries
 * @returns [[List]]
 * @example
 * ```ts
 * ```
 */
export declare type Omit<L extends List, K extends Key> = L extends unknown ? _Omit<L, K> : never;
