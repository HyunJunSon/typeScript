import { WritableKeys as OWritableKeys } from 'ts-toolbelt/out/Object/WritableKeys';
import { ObjectOf } from 'ts-toolbelt/out/List/ObjectOf';
import { List } from 'ts-toolbelt/out/List/List';
/**
 * Get the keys of `L` that are writable
 * @param L
 * @returns [[Key]]
 * @example
 * ```ts
 * ```
 */
export declare type WritableKeys<L extends List> = OWritableKeys<ObjectOf<L>>;
