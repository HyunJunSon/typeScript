import { Cast } from 'ts-toolbelt/out/Any/Cast';
import { OptionalPart } from 'ts-toolbelt/out/Object/Optional';
import { Depth } from 'ts-toolbelt/out/Object/_Internal';
import { List } from 'ts-toolbelt/out/List/List';
/**
 * Make `L` optional (deeply or not)
 * @param L to make optional
 * @param depth (?=`'flat'`) 'deep' to do it deeply
 * @returns [[List]]
 * @example
 * ```ts
 * ```
 */
export declare type Optional<L extends List, depth extends Depth = 'flat'> = Cast<OptionalPart<L, depth>, List>;
