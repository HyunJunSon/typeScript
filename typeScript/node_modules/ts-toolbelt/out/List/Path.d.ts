import { Path as OPath } from 'ts-toolbelt/out/Object/Path';
import { Key } from 'ts-toolbelt/out/Any/Key';
import { List } from 'ts-toolbelt/out/List/List';
/**
 * Get in `L` the type of nested properties
 * @param L to be inspected
 * @param Path to be followed
 * @returns [[Any]]
 * @example
 * ```ts
 * ```
 */
export declare type Path<L extends List, Path extends List<Key>> = OPath<L, Path>;
