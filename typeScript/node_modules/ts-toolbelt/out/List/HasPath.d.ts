import { HasPath as OHasPath } from 'ts-toolbelt/out/Object/HasPath';
import { Match } from 'ts-toolbelt/out/Any/_Internal';
import { Key } from 'ts-toolbelt/out/Any/Key';
import { ObjectOf } from 'ts-toolbelt/out/List/ObjectOf';
import { List } from 'ts-toolbelt/out/List/List';
/**
 * Check whether `L` has nested entries that match `M`
 * @param L to be inspected
 * @param Path to be followed
 * @param M (?=`any`) to check entry type
 * @param match (?=`'default'`) to change precision
 * @returns [[Boolean]]
 * @example
 * ```ts
 * ```
 */
export declare type HasPath<L extends List, Path extends List<Key>, M extends any = any, match extends Match = 'default'> = OHasPath<ObjectOf<L>, Path, M, match>;
