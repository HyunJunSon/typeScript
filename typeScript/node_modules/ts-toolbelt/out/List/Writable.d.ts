import { Depth } from 'ts-toolbelt/out/Object/_Internal';
import { WritablePart } from 'ts-toolbelt/out/Object/Writable';
import { List } from 'ts-toolbelt/out/List/List';
import { Cast } from 'ts-toolbelt/out/Any/Cast';
/**
 * Make `L` writable (deeply or not)
 * @param L to make writable
 * @param depth (?=`'flat'`) 'deep' to do it deeply
 * @returns [[List]]
 * @example
 * ```ts
 * ```
 */
export declare type Writable<L extends List, depth extends Depth = 'flat'> = Cast<WritablePart<L, depth>, List>;
