import { Range } from 'ts-toolbelt/out/Number/Range';
import { UnionOf } from 'ts-toolbelt/out/List/UnionOf';
/**
 * Create a set of keys
 * @param From to start with
 * @param To to end with
 * @returns [[Key]]
 * @example
 * ```ts
 * ```
 */
export declare type KeySet<From extends number, To extends number> = UnionOf<Range<From, To, '->'>>;
