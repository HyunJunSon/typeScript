import { NullableKeys as ONullableKeys } from 'ts-toolbelt/out/Object/NullableKeys';
import { ObjectOf } from 'ts-toolbelt/out/List/ObjectOf';
import { List } from 'ts-toolbelt/out/List/List';
/**
 * Get the keys of `L` that are nullable
 * @param L
 * @returns [[Key]]
 * @example
 * ```ts
 * ```
 */
export declare type NullableKeys<L extends List> = ONullableKeys<ObjectOf<L>>;
