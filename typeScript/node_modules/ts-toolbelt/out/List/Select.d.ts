import { Match } from 'ts-toolbelt/out/Any/_Internal';
import { Select as OSelect } from 'ts-toolbelt/out/Object/Select';
import { ListOf } from 'ts-toolbelt/out/Object/ListOf';
import { ObjectOf } from 'ts-toolbelt/out/List/ObjectOf';
import { List } from 'ts-toolbelt/out/List/List';
/**
 * Extract the entries of `L` that match `M`
 * @param L to extract from
 * @param M to select entries
 * @param match (?=`'default'`) to change precision
 * @returns [[List]]
 * @example
 * ```ts
 * ```
 */
export declare type Select<L extends List, M extends any, match extends Match = 'default'> = ListOf<OSelect<ObjectOf<L>, M, match>>;
