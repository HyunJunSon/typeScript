import { Depth } from 'ts-toolbelt/out/Object/_Internal';
import { ReadonlyPart } from 'ts-toolbelt/out/Object/Readonly';
import { List } from 'ts-toolbelt/out/List/List';
import { Cast } from 'ts-toolbelt/out/Any/Cast';
/**
 * Make `L` readonly (deeply or not)
 * @param L to make readonly
 * @param depth (?=`'flat'`) 'deep' to do it deeply
 * @returns [[List]]
 * @example
 * ```ts
 * ```
 */
export declare type Readonly<L extends List, depth extends Depth = 'flat'> = Cast<ReadonlyPart<L, depth>, List>;
