import { Exclude } from 'ts-toolbelt/out/Union/Exclude';
import { Last } from 'ts-toolbelt/out/Union/Last';
/**
 * Remove an item out of `U`
 * (⚠️ it might not preserve order)
 * @param U to remove from
 * @returns [[Union]]
 * @example
 * ```ts
 * ```
 */
export declare type Pop<U extends any> = Exclude<U, Last<U>>;
